function solve(input) {
    let result = []
    for (let index = 0; index < input.length; index++) {
        if (index % 2 != 0) {
            let num = input[index] * 2;
            result.push(num)
        }
        
    }
    result.reverse()
    return result.join(" ")
}
