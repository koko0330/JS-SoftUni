function solve(input) {
    finalString = ""
    for (let index = 0; index < input.length; index++) {
        if (index % 2 == 0) {
            finalString += input[index] + " ";
        }  
    }
    return finalString 
}
