function rectangle(width, height, color) {
    return {
        width,
        height,
        color: nameCapitalized = color.charAt(0).toUpperCase() + color.slice(1),
        calcArea() {
            return Number(this.width * this.height)
        }
    }
}
